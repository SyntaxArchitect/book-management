import BookForm from '../components/BookForm';

const AddBook = () => {
    return (
        <div>
            <h1>Add Book</h1>
            <BookForm onSubmit={() => { }} />
        </div>
    );
};

export default AddBook;
